# TweetsDisaster-NLP-
Disaster Tweet Detector is a web application built with FastAPI that classifies tweets as 'Disaster' or 'Not Disaster' using a custom-trained BERT model. The interface allows users to enter a tweet and get real-time predictions.
